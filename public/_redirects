# Netlify redirects configuration for SPA routing
# This ensures all routes are handled by Angular's router
# and preserves hash fragments for Netlify Identity

/*    /index.html   200

# Serve admin panel files (Netlify CMS) for /admin/* routes
/admin/*    /admin/index.html   200
